RewriteEngine On

# 1) Let the physical /install directory pass through untouched
RewriteRule ^install/ - [L]

# 2) Standard CI front-controller rewrite
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]
